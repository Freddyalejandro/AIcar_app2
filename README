para comenzar este programa despues de clonarlo 
1 abrir la carpeta backend y npm install express 
2 abrir la carpeta frontend y npm isntall expo

npx expo prebuild
eas --version
npx expo install @shopify/react-native-skia
npx expo install expo-device react-native-base64
react-native-ble-plx
npx create-expo-app -t expo-temple-blank-typescript expo-ble-sample

para lansar el servidor de expo de nuevo
npx expo start --dev-client

ejecutar en el dispositivo de nuevo 
npx expo run:android